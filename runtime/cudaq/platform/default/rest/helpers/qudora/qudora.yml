# ============================================================================ #
# Copyright (c) 2024 - 2024 NVIDIA Corporation & Affiliates.                   #
# All rights reserved.                                                         #
#                                                                              #
# This source code and the accompanying materials are made available under     #
# the terms of the Apache License 2.0 which accompanies this distribution.     #
# ============================================================================ #

name: qudora
description: "CUDA-Q target for Qudora."
config:
  # Tell DefaultQuantumPlatform what QPU subtype to use
  platform-qpu: remote_rest
  # Tell NVQ++ to generate glue code to set the target backend name
  gen-target-backend: true
  # Add the rest-qpu library to the link list
  link-libs: ["-lcudaq-rest-qpu"]
  # Define the JIT lowering pipeline
  jit-high-level-pipeline: "expand-measurements"
  jit-mid-level-pipeline: "quantinuum-gate-set-mapping"
  # Tell the rest-qpu that we are generating Adaptive QIR.
  codegen-emission: qir-adaptive
  # Library mode is only for simulators, physical backends must turn this off
  library-mode: false

target-arguments:
  - key: url
    required: false
    type: string
    platform-arg: url 
    help-string: "Specify the URL."
  - key: machine
    required: false
    type: string
    platform-arg: machine 
    help-string: "Specify QPU."
  - key: backend_settings
    required: false
    type: string
    platform-arg: backend_settings 
    help-string: "Additional settings for backend. See QUDORA Documentation for options."
